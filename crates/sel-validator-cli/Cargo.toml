[package]
name = "sel-validator-cli"
version = "1.0.0"
edition = "2024"
description = "SEL Validator CLI - Core 1.0 Compliant"

[dependencies]
sel-common = { path = "../sel-common" }
sel-validator = { path = "../sel-validator" }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
chrono = { workspace = true, features = ["serde"] }  # 👈 Added serde feature
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
